= SiteOption.mail_template('confirmation_mail').gsub('[[resource_email]]', @resource.email).gsub('[[unlock_account_url]]', link_to 'Unlock my account', unlock_url(@resource, :unlock_token => @resource.unlock_token))