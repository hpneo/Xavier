.box
	%h3.cufon1
		User
	%h2.cufon1
		=@user.id
	%p
		-if user_signed_in? && current_user.id==@user.id
			%a{:href => edit_user_registration_path(@user)} Edit profile
			You have spent
			= number_to_currency(current_user.paid_purchases_amount)
/ /box