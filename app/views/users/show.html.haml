.box
	%h3.cufon1
		User
	%h2.cufon1
		=@user.last_name
		,
		=@user.first_name
		=@user.last_name
		(H)
	%p
		-if user_signed_in? && current_user.id==@user.id
			%a{:href => edit_user_registration_path(@user)} Edit profile
/ /box